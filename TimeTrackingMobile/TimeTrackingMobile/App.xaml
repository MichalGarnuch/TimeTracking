<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TimeTrackingMobile.App">
    <!--
        Define global resources and styles here, that apply to all pages in your app.
    -->
    <Application.Resources>
        
        <ResourceDictionary>
            <Color x:Key="Primary">#007ACC</Color>
            <!-- lub dowolny inny kolor -->
        </ResourceDictionary>

        <!-- Kolory aplikacji -->
        <Color x:Key="PrimaryColor">#1F2937</Color>
        <!-- ciemnoszary -->
        <Color x:Key="AccentColor">#00BCD4</Color>
        <!-- turkusowy (material teal) -->
        <Color x:Key="LightTextColor">#FFFFFF</Color>
        <Color x:Key="PageBackgroundColor">#F1F5F9</Color>
        <!-- jasnoszary tło -->

        <!-- Styl dla nagłówków (większy, pogrubiony) -->
        <Style x:Key="HeaderLabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="Medium" />
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="TextColor" Value="{StaticResource PrimaryColor}" />
        </Style>

        <!-- Styl dla podstawowych etykiet -->
        <Style x:Key="BodyLabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="Small" />
            <Setter Property="TextColor" Value="#333333" />
        </Style>

        <!-- Styl dla przycisków (zaokrąglone, w kolorze Accent) -->
        <Style TargetType="Button">
            <Setter Property="BackgroundColor" Value="{StaticResource AccentColor}" />
            <Setter Property="TextColor" Value="{StaticResource LightTextColor}" />
            <Setter Property="CornerRadius" Value="20" />
            <Setter Property="HeightRequest" Value="45" />
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="Margin" Value="0,10,0,0" />
        </Style>

        <!-- Styl CollectionView / ListView itemów można dodać podobnie -->
        <!-- ... -->

    </Application.Resources>
</Application>
