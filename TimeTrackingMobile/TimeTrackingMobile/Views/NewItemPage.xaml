<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
    x:Class="TimeTrackingMobile.Views.NewItemPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Shell.PresentationMode="ModalAnimated"
    Title="New Item"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    ios:Page.UseSafeArea="true"
    BackgroundColor="{StaticResource PageBackgroundColor}">

    <StackLayout Spacing="15" Padding="15">
        <Label Text="Text" Style="{StaticResource HeaderLabelStyle}" />
        <Entry Text="{Binding Text, Mode=TwoWay}" FontSize="Medium" />

        <Label Text="Description" Style="{StaticResource HeaderLabelStyle}" />
        <Editor Text="{Binding Description, Mode=TwoWay}" AutoSize="TextChanges" FontSize="Small" Margin="0" />

        <StackLayout Orientation="Horizontal" Spacing="10">
            <Button Text="Cancel" Command="{Binding CancelCommand}" />
            <Button Text="Save" Command="{Binding SaveCommand}" />
        </StackLayout>
    </StackLayout>
</ContentPage>
